spring:
  datasource:
    username: root
    password: dsrm
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/dsrm
  jpa:
    hibernate:
      ddl-auto: update
  security:
    user:
      name: admin
      password: dsrm
dsrm:
  auth:
    jwtExpirationMs: 3600000
    jwtRefreshExpirationMs: 86400000